
<%= form_with model: [@topic, @topic_message] do |f|%>
  <%= f.text_area :body, rows: 5, class: 'form-control', placeholder: 'Комментарий' %>
  <%= f.submit "Отправить", class: "btn btn-default" %>
<% end %>
